const { simplify, parse, derivative } = require('mathjs')

module.exports = {
	name: 'mathfs',
	description: 'ngitung',
	hidden: false,
	checkOwner: false,
	run: (bot, message, args) => {
		message
	}
};